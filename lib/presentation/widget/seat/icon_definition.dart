import 'package:flutter/material.dart';

class IconDefinition extends StatelessWidget {
  const IconDefinition({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
