import 'package:flutter/material.dart';

class ContentUnavailable extends StatelessWidget {
  const ContentUnavailable({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
